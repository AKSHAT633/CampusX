import React from 'react'
import { useTheme } from "../context/ThemeContext"

const Chat = () => {
  
  const { isDark } = useTheme()
  return (
    <div>
      dsfds
    </div>
  )
}

export default Chat
